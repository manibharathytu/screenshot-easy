ends not coming - thats cos the shell scrpt is terminated not the node process
application already running not coming - also time not coming

the single-instance-app.sock error why? - its gon with sudo why?

printscreen crashed the app.

----------

closing - for now system monitor killing is the only way

--------
app log make it IST and readable