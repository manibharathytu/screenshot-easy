
the single-instance-app.sock error why? - its gon with sudo why?

printscreen crashed the app once. why

----------

closing - for now system monitor killing is the only way

--------
app log make it IST and readable

----

the sock err is not printed in the logs. i think because its stderr and i dint incude it in the log

-----------

service keeps on restarting when already an instance is runnnig